server:
  port: 9042

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: secret
  liquibase:
    change-log: classpath:db/liquibase-changelog-test.xml
    default-schema: public
    enabled: true
    drop-first: false
    password: secret
    user: admin